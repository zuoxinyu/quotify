# Provider Icons

SVG icons in this directory were copied from `steipete/CodexBar` `docs/logos`.
CodexBar is MIT licensed. MiMo does not currently have a matching CodexBar logo,
so the app renders a local fallback badge for that provider.
